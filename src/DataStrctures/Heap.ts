// TODO: implement data structures
// Take inspiration from: https://www.npmjs.com/package/@datastructures-js/priority-queue#size
export class MinHeap {
	constructor(comparator: (data: any) => number) {}
	dequeue = () => [1, 1];
	enqueue = (data: any) => true;
	size = () => -1;
	isEmpty = () => true;
}
